# AGENTS.md

本文件是 LinguaBridge 仓库的项目级协作规范。所有自动化 agent、代码助手和开发者在本仓库内工作时都应遵守。

## 1. 项目上下文

LinguaBridge 当前目标是构建面向中文技术学习者的 PC 端 AI 实时字幕与翻译伴学工具。

MVP 已确认：

- 首发 Windows 10/11 PC 客户端。
- 通过系统音频录制实现实时字幕，不以浏览器扩展作为主形态。
- 首发英文 -> 简体中文，预留多语种扩展。
- 先做实时字幕与字幕自动纠错，不首发语音同传。
- 音频、原文、译文、修订历史需要落盘，用于后续精读解析。
- Alpha 阶段使用邀请码 + 用量统计，不先接正式支付。
- MVP 阶段模型和云服务优先使用阿里云。

核心文档：

- `docs/prd-mvp-ai-realtime-subtitle.md`
- `docs/technical-selection-mvp.md`

## 2. 不确定事项必须询问

如果遇到以下情况，必须先问项目负责人，不要自行猜测：

- 会影响产品方向、商业模式、隐私合规或数据保留策略的决策。
- 会改变已确认 MVP 范围的决策，例如首发平台、语音同传、多语种、支付、企业版。
- 会引入新的云厂商、第三方模型、支付渠道或用户数据处理方。
- 会改变音频落盘、云端保存、删除机制或用户授权方式。
- 会明显增加成本、延迟、权限范围或上架风险的实现方案。
- 文档或代码中出现互相矛盾的需求。

如果只是局部实现细节，且已有文档能支撑判断，可以按现有技术选型执行，并在变更说明中记录假设。

## 3. 外部文档检索

当任务涉及库、框架、SDK、API、CLI 工具或云服务的用法时，必须通过 `ctx7` CLI 拉取当前文档，不要只凭记忆回答或实现。

步骤：

1. 解析库：`npx ctx7@latest library <name> "<user question>"`
2. 选择最匹配的 `/org/project` 或版本化 ID。
3. 拉取文档：`npx ctx7@latest docs <libraryId> "<user question>"`
4. 基于拉取到的文档回答或实现。

不要把 API Key、密码、token、连接串等敏感信息放入查询。

## 4. 技术选型基线

除非项目负责人确认变更，否则按以下基线推进：

- 桌面端：Tauri v2 + React + TypeScript。
- 原生能力：Rust 封装 Windows WASAPI loopback。
- 后端：TypeScript + Node.js 22 + Fastify + raw WebSocket。
- 模型链路：阿里云 ASR + Qwen-MT 为主，LiveTranslate 做 Spike。
- 数据库：PostgreSQL。
- 对象存储：阿里云 OSS。
- 缓存/限流：Redis 或阿里云 Tair（**MVP Alpha 已确认跳过**；当前阶段优先快速打通功能，不做限流、额度拦截、Redis 及后台运营能力）。
- 日志监控：阿里云 SLS + 应用指标。
- Alpha 部署：ECS + Docker Compose。

客户端不得直连阿里云模型服务；所有模型调用必须经过 Realtime Gateway。

## 5. 代码书写规范

### 5.1 通用

- 优先保持实现简单、清晰、可测试，不为未来功能过度抽象。
- 命名使用英文，业务文案和产品文档可以使用中文。
- 不提交密钥、token、证书、真实用户数据或含敏感内容的音频样本。
- 新增配置必须提供 `.env.example` 或文档说明，不提交真实 `.env`。
- 重要边界条件必须有测试或明确的手工验证步骤。
- 错误处理要能指导用户下一步操作，尤其是音频设备、网络和额度相关错误。

### 5.2 TypeScript

- 开启严格类型检查，不使用 `any` 绕过核心业务类型。
- 客户端/服务端共享的协议类型应集中维护，避免重复定义。
- WebSocket 事件必须有稳定的 `type` 字段和版本化 payload。
- 对外部输入做校验，包括邀请码、会话 ID、字幕事件和导出参数。

### 5.3 Rust

- 音频采集层保持独立模块，暴露稳定接口给 Tauri command。
- 对 WASAPI 设备切换、采样率转换、空音频、权限/设备错误做显式错误类型。
- 实时音频循环中避免阻塞 UI 线程。
- 长时间运行路径需要关注内存增长、句柄释放和线程退出。

### 5.4 数据与隐私

- 音频、字幕、修订历史落盘必须关联用户授权。
- 删除会话时必须删除本地缓存、数据库记录、OSS 对象和后续解析索引。
- 用量统计使用 append-only events，便于后续重算成本。
- 用户可见的历史会话与后台成本统计可以使用不同视图，但底层数据口径必须一致。

## 6. 文档规范

- 产品需求写入 `docs/prd-*.md`。
- 技术方案和选型写入 `docs/technical-*.md`。
- 阶段计划、Spike 结果和复盘可写入 `docs/notes-*.md` 或新增专题文档。
- 文档更新应同步维护 `docs/README.md`。
- 文档中的外部资料要尽量附官方链接。

## 7. Git 规范

- 提交前检查 `git status --short`，只提交与当前任务相关的文件。
- 不回滚用户未要求回滚的改动。
- Commit message 使用简洁英文，推荐格式：
  - `docs: add mvp prd and technical selection`
  - `feat: add realtime gateway skeleton`
  - `fix: handle audio device switch`
- 提交说明应能看出变更类型和范围。

## 8. PR（Pull Request）提交规范

- 新功能必须通过 PR 提交，不直接向主分支推送功能代码。
- 每个 PR 只做一件事：只实现或修改单一功能；鼓励尽可能小、粒度尽可能细的 PR；大功能应拆分为多个独立 PR 分步提交。
- PR 标题与描述需清晰完整，内容包含：
  - **标题：** 一句话说明本 PR 新增或修改了什么。
  - **功能描述：** 说明该功能的作用与使用方式。
  - **实现思路：** 简要说明技术选型或核心实现逻辑。
  - **测试方式：** 如何验证该功能正常运行。
- PR 合并后，主分支代码需保持可运行状态；评委在任意时间查看应能复现演示效果。

## 9. 当前优先级

当前优先级按顺序为：

1. Phase 0 技术 Spike：Windows WASAPI loopback、字幕浮窗、阿里云 ASR/Qwen-MT、LiveTranslate 对比。
2. Alpha 基础闭环：邀请码、用量统计、会话落盘、字幕事件协议。
3. 内测质量：延迟、术语、纠错、删除能力、成本统计。
